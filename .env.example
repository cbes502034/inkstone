# 後端 API 位址（本機開發指向 FastAPI，正式版指向 Render）
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Supabase —— anon key 是設計上可公開的前端金鑰，
# 真正的權限由資料庫的 Row Level Security 決定，service_role key 絕不可放前端
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
