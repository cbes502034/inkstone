# --- Web 框架 ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20          # 表單與檔案上傳（頭像）

# --- 資料庫 ---
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
asyncpg==0.30.0                   # 正式環境：Postgres（Supabase）
aiosqlite==0.20.0                 # 本機開發：免帳號直接跑

# --- 設定與驗證 ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- 身分驗證 ---
PyJWT==2.10.1
argon2-cffi==23.1.0               # 密碼雜湊。argon2id 是目前的建議選擇

# --- 快取與外部服務 ---
redis==5.2.1                      # AI 對話暫存（設 TTL，不落地資料庫）
httpx==0.28.1                     # 呼叫 Hugging Face 推論服務

# --- 圖片處理 ---
pillow==11.1.0                    # 頭像後端二次驗證與壓縮
